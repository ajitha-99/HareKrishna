public  interface RectangleInterface {
    
    void showResult();

    Decimal area();
    Decimal area(decimal length,decimal breadth);
    
    Integer area(Integer len,Integer brth);

    Decimal perimeter();
    Decimal perimeter(decimal length,decimal breadth);
}